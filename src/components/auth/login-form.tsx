"use client";
import { useState } from "react";

export function LoginForm({ csrfToken }: { csrfToken: string }) { const [error, setError] = useState(""); return <form className="mt-8 grid gap-4 rounded-xl border bg-surface p-6" onSubmit={async (event: any) => { event.preventDefault(); setError(""); const form = new FormData(event.currentTarget); const response = await fetch("/api/auth/login", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ email: form.get("email"), password: form.get("password"), csrfToken }) }); if (response.ok) window.location.assign("/dashboard"); else setError("Invalid email or password."); }}><label className="text-sm font-medium">Email<input name="email" type="email" required className="mt-2 min-h-12 w-full rounded-md border bg-background px-4" /></label><label className="text-sm font-medium">Password<input name="password" type="password" required className="mt-2 min-h-12 w-full rounded-md border bg-background px-4" /></label><button className="min-h-12 rounded-md bg-primary px-6 font-semibold text-primary-foreground">Sign in securely</button>{error ? <p role="alert" className="text-sm text-error">{error}</p> : null}<p className="text-sm text-foreground/65">OAuth provider buttons can be enabled when provider client IDs are configured.</p></form>; }
