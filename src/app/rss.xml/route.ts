export async function GET() { const base = process.env.NEXT_PUBLIC_SITE_URL ?? "https://prometheus.example"; const xml = `<?xml version="1.0" encoding="UTF-8"?><rss version="2.0"><channel><title>Project Prometheus</title><link>${base}</link><description>Transparent impact updates from Project Prometheus.</description></channel></rss>`; return new Response(xml, { headers: { "Content-Type": "application/rss+xml; charset=utf-8", "Cache-Control": "public, max-age=3600" } }); }
