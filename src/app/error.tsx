"use client";
export default function Error({ reset }: { error: Error & { digest?: string }; reset: () => void }) { return <main className="grid min-h-screen place-items-center bg-background px-6 text-foreground"><section className="max-w-md rounded-xl border bg-surface p-8 shadow-card"><h1 className="font-display text-4xl font-semibold">Something went wrong.</h1><p className="mt-4 text-foreground/70">The page could not be loaded. Please try again.</p><button className="mt-6 min-h-11 rounded-md bg-primary px-5 font-semibold text-primary-foreground" onClick={reset}>Try again</button></section></main>; }
