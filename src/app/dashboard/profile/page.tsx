import { Avatar } from "@/components";
import { requireUser } from "@/lib/auth/session";
export default async function ProfilePage() { const user = await requireUser(["donor", "volunteer", "administrator"]); return <section className="max-w-3xl"><h1 className="font-display text-5xl font-semibold">Profile</h1><div className="mt-8 rounded-xl border bg-surface p-6"><div className="flex items-center gap-4"><Avatar name={user!.name} src={user!.avatarUrl} /><div><h2 className="font-semibold">{user!.name}</h2><p className="text-sm text-foreground/65">{user!.email}</p></div></div><dl className="mt-8 grid gap-4 sm:grid-cols-3"><div><dt className="text-sm text-foreground/60">Newsletter</dt><dd className="font-semibold">{user!.preferences.newsletter ? "Enabled" : "Disabled"}</dd></div><div><dt className="text-sm text-foreground/60">Donation receipts</dt><dd className="font-semibold">{user!.preferences.donationReceipts ? "Enabled" : "Disabled"}</dd></div><div><dt className="text-sm text-foreground/60">Volunteer updates</dt><dd className="font-semibold">{user!.preferences.volunteerUpdates ? "Enabled" : "Disabled"}</dd></div></dl></div></section>; }
