import { getDonationHistory } from "@/lib/payments/donations";
export default async function AdminPage() { const donations = await getDonationHistory("admin"); return <section><p className="text-sm font-semibold uppercase tracking-[.18em] text-foreground/60">Admin</p><h1 className="mt-4 font-display text-5xl font-semibold">Operations overview</h1><div className="mt-8 grid gap-4 md:grid-cols-2 xl:grid-cols-4">{[{ label: "Donations", value: donations.length }, { label: "Volunteers", value: 1 }, { label: "Content items", value: 18 }, { label: "Recent activity", value: 7 }].map((item) => <article key={item.label} className="rounded-lg border bg-surface p-6"><h2 className="font-semibold">{item.label}</h2><p className="mt-3 font-display text-4xl font-semibold">{item.value}</p></article>)}</div></section>; }
