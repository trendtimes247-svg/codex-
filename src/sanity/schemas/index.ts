import { faq, footer, gallery, globalSettings, homepage, navigation, news, partner, person, program, project, report, story, testimonial, event } from "./documents";
import { link, seo, stat, timelineItem } from "./objects";

export const schemaTypes = [seo, link, stat, timelineItem, homepage, program, project, story, news, event, person, partner, testimonial, report, faq, gallery, globalSettings, navigation, footer];
